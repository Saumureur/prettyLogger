export { BasicLogger} from "./BasicLogger";

/* import { BasicLogger } from "./BasicLogger";

const myLogger = new BasicLogger("src/config/config.json");

const message = {
  message: "This is a log message",
  object: [1, "two", { three: 3 }],
  level:"info"
};

myLogger.log(message);
console.log(myLogger.config); */